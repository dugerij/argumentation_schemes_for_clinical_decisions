# Copy to .env if you prefer loading run settings from a file.
# Never store HF_TOKEN here; use `hf auth login` or a shell secret.

HF_BUCKET=your-hf-username/jobs-artifacts
HF_IMAGE=vllm/vllm-openai:v0.18.1
RUN_SCOPE=validation
RUN_PHASE=comparison
RUN_ID=validation-yyyymmddthhmmssz
UMLS_DB=output/cache/umls_local.sqlite3
